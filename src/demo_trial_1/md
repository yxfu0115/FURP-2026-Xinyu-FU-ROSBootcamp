┌─────────────────────────────────────────────────────────────┐
│ 外部控制端                                                   │
│  ros2 topic pub /cmd_vel Twist{linear.x, angular.z}          │
└───────────────────────┬─────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ MuJoCo仿真节点 mujoco_sim_node                               │
│ 输入：/cmd_vel                                               │
│ 内部计算：                                                   │
│   1. 差分轮速度分解 v_left / v_right → 电机控制量 ctrl        │
│   2. mj_step 物理步进，获取真实轮速 qvel                     │
│   3. 轮式里程积分：odom_x / odom_y / odom_yaw                │
│ 输出话题：                                                   │
│   ① /clock 仿真时钟（100Hz，use_sim_time=true时全局时序基准） │
│   ② /odom nav_msgs/Odometry（50Hz，底盘位姿+速度）            │
│   ③ /scan sensor_msgs/LaserScan（10Hz，360线雷达数据）        │
│ 输出TF变换：                                                 │
│   1. 动态TF：odom → carter_mini_base（50Hz，里程计位姿）     │
│   2. 静态TF：carter_mini_base → lidar_link（仅启动发一次）    │
└───────────────────┬───────────────────────────┬──────────────┘
                    ↓                           ↓
          ┌────────────────┐           ┌─────────────────────┐
          │ /clock 时序流  │           │ /scan 激光观测流     │
          └────────┬───────┘           └──────────┬──────────┘
                   ↓                              ↓
┌─────────────────────────────────────────────────────────────┐
│ async_slam_toolbox_node（SLAM核心）                          │
│ 订阅：                                                       │
│   1. /clock （use_sim_time=true时，所有查询基于仿真时间）     │
│   2. /scan 激光扫描数据                                      │
│ TF查询依赖：                                                 │
│   查询 odom → carter_mini_base 变换，用于激光点云去畸变       │
│ 输出：                                                       │
│   1. map → odom 全局TF（地图矫正）                           │
│   2. /map 栅格地图话题                                       │
└─────────────────────────────────────────────────────────────┘
